{
  "name": "mcp-postgres-dump-schema",
  "version": "0.0.2",
  "description": "MCP server for executing pg_dump --schema-only",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-postgres-dump-schema": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && shx chmod +x dist/index.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "postgres",
    "postgresql",
    "schema",
    "dump",
    "pg_dump"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sunnyone/mcp-postgres-dump-schema.git"
  },
  "license": "MIT"
}
